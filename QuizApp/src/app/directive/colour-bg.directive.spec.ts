import { ColourBgDirective } from './colour-bg.directive';

describe('ColourBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ColourBgDirective();
    expect(directive).toBeTruthy();
  });
});
